export interface TableData {
  teamName: string;
  judge: string;
  score1: number; score2: number; score3: number; score4: number;
  sum: number;
  internal_feedback: string;
  external_feedback: string;
}
